# Copy to .env (never commit .env). Every stack reads the same variables.
AMN_ENV=sandbox                 # sandbox | live
AMN_API_KEY=your-sandbox-api-token
AMN_WEBHOOK_SECRET=             # secret_key returned when you register a webhook
AMN_TIMEOUT_MS=20000
