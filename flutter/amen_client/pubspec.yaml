name: amen_client
description: Reference Dart client for the Amen Platform API (escrow deals, payments, webhooks).
version: 0.1.0
publish_to: none
environment:
  sdk: ^3.3.0
dependencies:
  http: ^1.2.0
  crypto: ^3.0.3
dev_dependencies:
  test: ^1.25.0
  lints: ^4.0.0
